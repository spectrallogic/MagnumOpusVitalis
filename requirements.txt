torch>=2.6.0,<3
transformers>=4.45.0,<5
numpy>=1.24.0
flask>=3.0.0
opencv-python>=4.8.0
websockets>=12.0
# The launcher uses CPU if a complete model cannot fit on the selected GPU.
# Pre-quantized/GGUF checkpoints require an additional engine adapter.
